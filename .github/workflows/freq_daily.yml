name: FREQ SHORTS — Daily Pipeline

on:
  schedule:
    - cron: '0 1 * * *'    # Her gün 01:00 UTC
  workflow_dispatch:        # Manuel tetikleme
    inputs:
      hz:
        description: 'Belirli bir Hz değeri (boş bırakılırsa otomatik seçilir)'
        required: false
        default: ''
      dry_run:
        description: 'Dry run (upload olmadan test)'
        required: false
        default: 'false'
        type: boolean

jobs:
  check-active-system:
    runs-on: ubuntu-latest
    outputs:
      should-run: ${{ steps.check.outputs.should-run }}
    steps:
      - name: Check if FREQ SHORTS is active
        id: check
        run: |
          ACTIVE=${{ vars.ACTIVE_SYSTEM }}
          if [[ "$ACTIVE" == "Hz" ]]; then
            echo "should-run=true" >> $GITHUB_OUTPUT
          else
            echo "should-run=false" >> $GITHUB_OUTPUT
            echo "⚫ FREQ SHORTS not active. ACTIVE_SYSTEM=$ACTIVE"
          fi

  run-freq-pipeline:
    needs: check-active-system
    if: |
      needs.check-active-system.outputs.should-run == 'true' ||
      github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Restore used freq cache
        uses: actions/cache@v4
        with:
          path: freq_used_topics.json
          key: freq-used-topics-${{ github.run_id }}
          restore-keys: |
            freq-used-topics-

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y ffmpeg imagemagick fonts-liberation fonts-noto-core

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Run FREQ SHORTS pipeline
        run: |
          HZ_ARG=""
          if [ -n "${{ github.event.inputs.hz }}" ]; then
            HZ_ARG="--hz ${{ github.event.inputs.hz }}"
          fi
          DRY_ARG=""
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            DRY_ARG="--dry-run"
          fi
          python freq_main.py $HZ_ARG $DRY_ARG
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          YOUTUBE_TOKEN_JSON: ${{ secrets.YOUTUBE_TOKEN_JSON }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          USE_FREQ_QUEUE: ${{ vars.USE_FREQ_QUEUE || 'false' }}

      - name: Save used freq cache
        if: always()
        uses: actions/cache@v4
        with:
          path: freq_used_topics.json
          key: freq-used-topics-${{ github.run_id }}

      - name: Commit freq_used_topics.json (kalıcı dedup)
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add freq_used_topics.json
          git diff --cached --quiet || git commit -m "chore: update freq_used_topics after daily run [skip ci]"
          git pull --rebase origin $(git rev-parse --abbrev-ref HEAD) || true
          git push origin HEAD || true

      - name: Upload output artifacts (debug)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: freq-pipeline-output-${{ github.run_id }}
          path: output/
          retention-days: 3
